# Automatically added by dh_installinit
if [ -x "/etc/init.d/ndn-mumble-server" ]; then
	update-rc.d ndn-mumble-server defaults 95 >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d ndn-mumble-server start || exit $?
	else
		/etc/init.d/ndn-mumble-server start || exit $?
	fi
fi
# End automatically added section
